# Copy to env.ll-collector on the *arr server (same directory as collect_ill_daily.py).
# chmod 600 env.ll-collector — never commit real tokens.
#
# TURSO_DATABASE_URL=libsql://hurryupandwait-cinkadeus.aws-us-east-1.turso.io
# TURSO_AUTH_TOKEN=<read-write token>